[pytest]
testpaths = e2e
python_files = test_*.py
addopts = -ra --strict-markers
# Les pages lentes (Vite au premier demarrage, migrations) depassent le
# defaut de 5 secondes.
timeout = 180
filterwarnings =
    ignore::DeprecationWarning
